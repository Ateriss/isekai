

function Shoppined(props: {styled:string}){
    const { styled }= props;
    return(
        <section className={`${styled}`} >
        acltualmente no has realizado ninguna compra</section>
    )
}
export { Shoppined }